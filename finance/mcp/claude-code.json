{
  "mcpServers": {
    "ibkr": {
      "_comment": "Paste into ~/.claude.json for all projects, or a project .mcp.json. On first use Claude Code opens IBKR's OAuth login in a browser; approve the mcp.read scope only, since these skills never write.",
      "type": "http",
      "url": "https://api.ibkr.com/v1/api/mcp-public"
    }
  }
}
